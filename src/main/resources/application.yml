jwt:
  secret: StringUsadaParaGerarToken
  expiration: 604800000
spring:
  application:
    name: auth-service
  datasource:
    url: jdbc:mysql://${DB_HOST:localhost:3306}/${DB_NAME:db_user}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:123}
server:
  port: ${PORT:8081}


